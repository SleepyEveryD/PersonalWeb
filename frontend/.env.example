# Base URL of the backend API.
# Local dev: point at the backend running on :8787.
# Render: set to the deployed backend Web Service URL (e.g. https://personalweb-api.onrender.com).
VITE_API_URL=http://localhost:8787
